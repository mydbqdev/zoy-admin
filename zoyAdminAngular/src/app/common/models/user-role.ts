import { AppRole } from "src/app/setting/user-master/models/app-role-model";

export class UserRole{
    empCode ?: string;
    empName ?: string;
    roles ?: string;
    empMail ?: string;
    roleModel ?: AppRole[];
    applicationNames ?: string;

}